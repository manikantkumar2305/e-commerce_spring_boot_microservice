apiVersion: v1
kind: Service
metadata:
  name: {{ include "server-registry.fullname" . }}
  labels:
    {{- include "server-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "server-registry.selectorLabels" . | nindent 4 }}
