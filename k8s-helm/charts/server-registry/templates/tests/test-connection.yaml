apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "server-registry.fullname" . }}-test-connection"
  labels:
    {{- include "server-registry.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "server-registry.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
