apiVersion: apps/v1
kind: Deployment

metadata:
  name: payment-service
  namespace: ecommerce

spec:
  replicas: 1

  selector:
    matchLabels:
      app: payment-service

  template:
    metadata:
      labels:
        app: payment-service

    spec:
      containers:
        - name: payment-service
          image: rajni2209/payment-service:v1

          ports:
            - containerPort: 9098

          env:
            - name: DB_HOST
              value: mysql

            - name: DB_PORT
              value: "3306"

          envFrom:
            - configMapRef:
                name: ecommerce-config

            - secretRef:
                name: ecommerce-secret